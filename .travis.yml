sudo:
  - required

services:
  - docker

language: python

script:
  - pwd
  - ls -la
#  - python -m pytest -v

#after_success:

  - ls .travis/docker_dockerhub.sh
  - cat .travis/docker_dockerhub.sh
#  - sudo /bin/sh .travis/deploy_dockerhub.sh
